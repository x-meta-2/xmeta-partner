/**
 * Performance statistics page — types only.
 *
 * Backend endpoints will be wired here once available. For now this page
 * derives its data from referrals + commissions endpoints.
 */

export type { MonthlyPerformance, PerformanceStats } from './types';
